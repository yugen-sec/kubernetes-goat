apiVersion: batch/v1
kind: Job
metadata:
  name: batch-check-job
spec:
  template:
    metadata:
      name: batch-check-job
    spec:
      containers:
      - name: batch-check
        image: loremitprodcr.azurecr.io/goat/k8s-goat-batch-check
        tag: v1
        # command:
        #  - "bin/sh"
        #  - "-c"
        #  - "htop"
      restartPolicy: Never
      